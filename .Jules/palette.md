## 2024-05-20 - Adding Clear Buttons to Custom Inputs
**Learning:** Adding a clear button to custom search inputs is a common and high-impact UX win, but it requires ensuring the input has sufficient right-padding so the text does not overlap with the absolute positioned clear button.
**Action:** Always add `paddingRight` to text inputs when overlaying an absolute positioned button inside them.
