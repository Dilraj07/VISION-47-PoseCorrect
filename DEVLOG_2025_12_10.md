# Developer Log - December 10, 2025

## 🎯 Objectives
- Polish UI for mobile responsiveness.
- Deploy full stack application to AWS EC2 (Free Tier).

## ✅ Completed Tasks

### 1. UI/UX Polish
- **Mobile Navbar**: Implemented "Dumbbell Menu" for better mobile navigation.
- **Auth Page**: Updated text to "Need a GymBro?".
- **Dashboard**: Marked *Bench Press*, *Lunge*, and *Bicep Curl* as `In Progress` (visual dimming + "SOON" badge).

### 2. Infrastructure & Deployment (AWS)
- **Architecture**: Pivoted to hosting both Frontend (React) and Backend (FastAPI) on a single AWS EC2 instance using Docker and Nginx.
- **Dockerization**:
  - Created `server/Dockerfile` for the backend.
  - Fixed `libgl1` dependency issue for Debian Bookworm support.
- **Frontend Config**:
  - Updated `vite.config.js` to use root base path `/` for production.
  - Configured API proxying in Nginx to route `/analyze` to the backend container.

### 3. Troubleshooting & Fixes
- **Memory Optimization**: Created `add_swap.sh` to add 2GB swap space, preventing OOM crashes on `t2.micro` (1GB RAM) instances.
- **Repository Hygiene**:
  - Cleaned up >100MB of test video files (`.mp4`) from the repo.
  - Added `server/.gitignore` to prevent future media bloat.

## 🚧 Current Status & Next Steps
- **Deployment Status**: Paused.
- **Issue**: The AWS instance disk reached 100% capacity due to repeated uploads of `node_modules` and cached Docker build layers.
- **Resolution Plan**:
  1. Terminated the clogged instance.
  2. Next session: Launch a fresh EC2 instance.
  3. Clone purely from GitHub (avoids uploading local junk).
  4. Run the streamlined build process.
